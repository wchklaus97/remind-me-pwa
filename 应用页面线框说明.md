# Remind Me PWA – 应用页面线框（Markdown）

本文档描述提供的设计图中的**应用页面与 UI 元素**。  
包含**每个页面**的结构块，以及**每个按钮/导航/交互元素**的功能说明。

---

## 当前进度（实现状态）

- **仪表盘**：搜索、筛选、统计、提醒列表与卡片、顶部操作、FAB 已完成
- **日历**：月/周视图切换已完成；选中日期列表/未排程列表已完成；拖拽改期（桌面 + 触摸）已完成
- **分类/标签**：管理与新增/编辑/删除/排序已完成；触摸拖拽排序已完成
- **提醒详情**：详情弹窗与编辑/完成/删除操作已完成
- **底部导航与设置**：底部导航与设置页面已完成

---

## 全局导航（底部 Tab 栏）

多数页面底部可见。

- **首页**（图标 + 文案）
  - 操作：进入**仪表盘 / 提醒列表**
- **日历**（图标 + 文案）
  - 操作：进入**日历视图**
- **分类**（图标 + 文案）
  - 操作：进入**分类管理**
- **设置**（图标 + 文案）
  - 操作：进入**设置**（设计图未展示）

### 路由与 PWA 约束
- 本项目为 **PWA**，应用主入口应保持在 **`/app`**。
- 页面切换应使用 **应用内状态/路由**，不要强制变更 URL 为其它路径。
- 如需区分子页面，可使用内部路由状态或锚点，但应 **保留 `/app` 作为稳定入口**。

---

## 页面 1 — 仪表盘（移动端）

**目标：** 概览提醒、快速筛选与统计。

### 布局模块
1. **顶部栏**
2. **搜索栏**
3. **筛选标签**
4. **统计卡片（2 张）**
5. **提醒列表**
6. **悬浮新增按钮（FAB）**
7. **底部导航栏**

### 元素与交互
- **头像/个人按钮**
  - 操作：打开个人中心或菜单
- **搜索输入框**
  - 占位： “搜索提醒...”
  - 操作：按关键词过滤列表
- **筛选标签**
  - **全部**（默认）
  - **今天**
  - **即将到期**
  - 操作：按时间范围过滤
- **统计卡片：今日完成**
  - 显示数量（如 “12”）
  - 操作：可选，点击过滤已完成
- **统计卡片：待处理**
  - 显示数量（如 “4”）
  - 操作：可选，点击过滤待处理
- **分组标题：提醒**
  - **排序按钮**
  - 操作：展开排序选项（时间、优先级、分类）
- **提醒卡片**
  - **分类标签**（如 WORK / PERSONAL）
  - **标题**
  - **信息行**（日期/时间等）
  - **状态点**
  - 操作：点击进入**提醒详情**
- **悬浮 “+”**
  - 操作：进入**新增提醒**

### 限制与注意事项
- 列表项必须保持 **触摸目标 ≥ 48x48px**。
- 搜索与筛选不应造成页面跳转，应仅在 `/app` 内更新视图状态。
- 排序操作避免阻塞 UI，建议异步或本地快速排序。

---

## 页面 2 — 新增提醒

**目标：** 新建提醒，包含标题、详情、时间、优先级与分类。

### 布局模块
1. **顶部返回**
2. **标题输入**
3. **详情说明**
4. **日期 / 时间**
5. **优先级选择**
6. **分类选择**
7. **主按钮**

### 元素与交互
- **返回按钮**
  - 操作：返回上一页
- **标题输入框**
  - 标签：提醒标题
  - 操作：输入提醒名称
- **详情文本域**
  - 标签：备注与详情
  - 操作：输入说明
- **日期选择器**
  - 操作：选择日期
- **时间选择器**
  - 操作：选择时间
- **优先级按钮**
  - **低 / 中 / 高**
  - 操作：选择一个优先级
- **分类标签**
  - 例如：个人 / 工作 / 购物
  - **新增分类**
  - 操作：选择分类或新建
- **保存按钮**
  - 操作：保存提醒并返回列表

### 限制与注意事项
- 日期/时间选择需兼容 **移动端系统原生控件**。
- 提交后必须 **立即回到 `/app` 内部列表**，不改变 URL 路径。
- 输入验证失败时给出明确错误提示（不弹出新页面）。

---

## 页面 3 — 提醒详情

**目标：** 查看提醒详情、倒计时与操作按钮。

### 布局模块
1. **顶部栏（返回 + 标题 + 更多）**
2. **倒计时模块**
3. **信息卡片**
4. **备注区域**
5. **操作按钮**

### 元素与交互
- **返回按钮**
  - 操作：返回上一页
- **标题**
  - 例如：Board Presentation
- **更多按钮**
  - 操作：打开菜单（编辑 / 删除 / 分享）
- **倒计时与圆环**
  - 展示剩余时间
- **优先级标签**
  - 例如：高
- **分类标签**
  - 例如：工作
- **时间信息**
  - 日期与时间
  - 提醒提前通知设置
- **备注内容**
  - 只读展示
- **编辑按钮**
  - 操作：进入编辑页面
- **完成按钮**
  - 操作：标记为已完成

### 限制与注意事项
- 倒计时更新需避免高频重排，建议 **节流或逐秒更新**。
- 编辑/完成动作完成后 **返回 `/app` 内部状态**，不改变 URL。
- “更多”菜单需可键盘访问并具备 ARIA 标签。

---

## 页面 4 — 日历视图

**目标：** 按日期查看提醒，支持月/周模式。

### 布局模块
1. **月份栏**
2. **日历网格**
3. **月/周切换**
4. **选中日期信息**
5. **当天提醒列表**
6. **底部导航栏**

### 元素与交互
- **月份切换**
  - 左右箭头切换月份
  - 月份下拉
  - 操作：切换月
- **日历日期格子**
  - 操作：选择日期
  - 提醒标记（小点/高亮）
- **月 / 周切换**
  - 操作：切换模式
- **选中日期标题**
  - 例如：Friday, Dec 15
  - **提醒数标签**
- **提醒条目**
  - 时间、标题、分类标签
  - 操作：进入详情

### 限制与注意事项
- 日历网格在小屏需保证可读性，必要时切换到周视图。
- 日期切换应平滑过渡，不触发完整页面刷新。
- 进入详情后返回应恢复到原日期与滚动位置。

---

## 页面 5 — 分类管理

**目标：** 管理分类、拖拽排序、删除分类。

### 布局模块
1. **顶部标题 + 新增按钮**
2. **提示说明**
3. **分类列表**
4. **新增分类按钮**
5. **底部导航栏**

### 元素与交互
- **新增 “+”**
  - 操作：新建分类
- **分类卡片**
  - **图标**
  - **名称**
  - **数量**
  - **拖拽手柄**
  - **删除按钮**（滑出/激活显示）
  - 操作：进入该分类提醒列表
- **创建新分类按钮**
  - 操作：进入新增分类表单

### 限制与注意事项
- 拖拽排序需兼容触摸设备，并提供明显拖拽手柄。
- 删除动作需二次确认或撤销机制，避免误删。
- 进入分类详情后返回仍应停留在 `/app` 上下文。

---

## 交互与可访问性要求

- 触摸目标 **≥ 48x48px**
- 图标按钮需 **ARIA 标签**
- 键盘可见焦点
- 文字对比度符合 **WCAG AA**

---

## 页面流转图

```text
仪表盘 -> 新增提醒
仪表盘 -> 提醒详情
仪表盘 -> 日历
仪表盘 -> 分类
日历 -> 提醒详情
分类 -> 分类列表 -> 提醒详情
```

---

## 可选扩展（后续）

- 提醒卡片滑动快捷操作（完成/删除）
- 搜索栏快速新增
- 分类批量操作
- 高级筛选（标签/优先级/时间）

